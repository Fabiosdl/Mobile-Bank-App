#connection to mariadb / phpMyAdmin using local machine
#spring.datasource.url=jdbc:mariadb://127.0.0.1:3306/mobilebankapp

#connection to MySQL Workbench
spring.datasource.url=jdbc:mysql://localhost:3306/mobilebankapp

# Use MariaDB driver
#spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

spring.datasource.username=springstudent
spring.datasource.password=springstudent

# Hibernate Dialect (either MySQL or MariaDB dialect)
#spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect

# command to create databases everytime the app runs. DB script is in resources directory.
#spring.sql.init.mode=always

# server.error.include-message: since version 2.3, Spring Boot hides the message field in the response
# to avoid leaking sensitive information; we can use this property with an always value to enable it.
server.error.include-message=always

# This tells Flyway that my existing database schema should be treated as version 1
#spring.flyway.baseline-on-migrate=true
#spring.flyway.baseline-version=1

#spring.flyway.repair=true

